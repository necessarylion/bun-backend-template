{
  "name": "adonis-bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run --watch server.ts",
    "build": "bun build ./server.ts --minify --sourcemap --compile --outfile ./server",
    "start": "./server"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2",
    "typedi": "^0.10.0",
    "winston": "^3.17.0"
  }
}
